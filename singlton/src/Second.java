/*Через enum
 * `Enum` в Java представляет собой перечисление (набор) констант.
 * Оно используется для определения типов данных, которые содержат некоторое ограниченное число объектов.
*/

//В этой реализации используется перечисление (enum) для создания единственного экземпляра класса.
// Поле "INSTANCE" является единственным экземпляром класса и доступно всегда.

public enum Second {
    INSTANCE
}